---
description: No secret ever reaches git — .gitignore patterns + pre-commit + pre-push guards on every operator repo.
globs:
alwaysApply: true
---

# Secret hygiene — no secret ever reaches git, on any repo

## The verbatim source (stated by Avery 2026-06-05)

> we have the proper .git files to hide important things? igone? … approved. proceed … mend them all

Said while standing up the Network Trust Spine, after an audit found `containers/.env.dgx` tracked and the `.gitignore` missing key/cert/tunnel patterns.

## The rule

In **every** operator repo, secrets are kept out of git by **three** layers:

1. **Ignore by pattern.** `.gitignore` ignores all env files + every key/cert/tunnel shape, keeping `*.example` templates:
   ```gitignore
   .env
   .env.*
   *.env
   !*.env.example
   !.env*.example
   !.env*.sample
   *.key
   *.pem
   *.crt
   *.p12
   *.pfx
   id_rsa*
   id_ed25519*
   wg*.conf
   wireguard*.conf
   *-privkey*
   ca.key
   ```
2. **Block at commit.** `.githooks/pre-commit` refuses secret-shaped files (catches `git add -f`) and runs `gitleaks protect --staged` when present. Wired via `git config core.hooksPath .githooks` (`make hooks` / `scripts/install-git-hooks.sh`).
3. **Block at push.** `.githooks/pre-push` runs `gitleaks protect` over the pushed range — local, not CI.

Every committed secret-bearing config gets an `<name>.example` twin with secret-named values blanked to `CHANGEME`.

## When this fires

- A new operator repo, or an audit of an existing one.
- A `.env`, key, cert, or WireGuard config is introduced.
- Fresh-machine onboarding — run `make hooks` (core.hooksPath is per-clone, uncommitted).

## Why

`.gitignore` alone does nothing for already-tracked files and is bypassed by `git add -f`. The commit + push guards close both holes with no network or CI.

## Propagation

Canonical body: `nephew/.claude/rules/secret-hygiene.md`. Mirrored to every operator repo's `.claude/` + `.cursor/`. Implementation lands per-repo.
