version: '3.9'

services:
  caddy:
    image: caddy:2
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy-data:/data
    networks:
      - control-net
      - voice-net
    ports:
      - "80:80"
      - "443:443"

volumes:
  caddy-data: